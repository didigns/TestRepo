# Gemma 4 E4B vs E2B 성능 분석 리포트

> 대상: LevA AISummary (로컬 파일 비서 · llama.cpp/llama-server · 기본 CPU 추론 · 한국어)
> 목적: 현재 메인 모델(Gemma 4 **E4B** Q4_K_M)을 **E2B**로 낮췄을 때의 성능·메모리·속도 트레이드오프 판단
> 기준일: 2026-07-06 · 출처: Google 모델카드 및 공개 벤치마크(하단 링크)

---

## TL;DR (요약)

- **E2B는 E4B 대비 CPU에서 약 1.5~2배 빠르고, 메모리는 절반 수준**(Q4 기준 ~2-3GB vs ~4-5GB)이다. 이 앱은 기본이 CPU 추론(-ngl 0)이라 속도 이득이 그대로 체감된다.
- **품질은 E4B가 확실히 위**다. 지식·추론(MMLU-Pro) 기준 약 **+9%p**, 멀티모달(이미지 이해)은 **+8%p** 이상 앞선다.
- **결론:** 16GB급 저사양 + 응답 속도 우선이면 **E2B**, 한국어 요약 정확도·이미지 OCR 품질 우선이면 **E4B** 유지. 앱이 모델 경로 교체만으로 전환 가능하니, **E2B로 먼저 체감 테스트 후 품질이 부족하면 E4B로 복귀**하는 실험을 권장한다.

---

## 1. 모델 스펙 비교

| 항목 | Gemma 4 **E2B** | Gemma 4 **E4B** |
|---|---|---|
| 유효 파라미터(effective) | 2.3B | 4.5B |
| 총 파라미터(disk 상 실제) | 5.1B | 8B |
| 원본 크기(비양자화) | ~7.2GB | ~9.6GB |
| Q4_K_M 크기(이 앱 기준) | ~2.5-3.5GB | **5.4GB**(현재 사용 중) |
| Q4 구동 RAM(대략) | ~2-3GB (모바일 포맷 ~1GB) | ~4-5GB |
| 컨텍스트 | 128K | 128K |
| 입력 모달리티 | 텍스트·이미지·오디오 | 텍스트·이미지·오디오 |
| 출시 | 2026-04-02 (Google DeepMind) | 2026-04-02 |

> "E"는 effective(유효) 파라미터. Per-Layer Embeddings(PLE) 기법으로 디스크 상 크기는 크지만 런타임에는 더 작은 모델처럼 동작한다. 그래서 E2B가 "총 5.1B"인데도 실행 시 2B급으로 가볍다.

---

## 2. 벤치마크 (품질)

공개 벤치마크/모델카드 집계 기준. 절대 수치는 출처마다 편차가 있으니 **상대 격차** 위주로 본다.

| 벤치마크 | E2B | E4B | 격차(E4B 우위) |
|---|---|---|---|
| MMLU-Pro (지식·추론) | 60.0% | 69.4% | **+9.4%p** |
| AIME 2026 (수학) | 37.5% | 42.5% | +5.0%p |
| LiveCodeBench v6 (코딩) | 44.0% | 52.0% | +8.0%p |
| MMMU-Pro (멀티모달/이미지) | 44.2% | 52.6% | **+8.4%p** |

해석: E4B가 전 영역에서 앞서며, 특히 **복합 추론과 이미지 이해에서 격차가 크다**. E2B도 소형 모델치고 강하지만, "한 번에 깊게 생각해야 하는" 요약·분석에서 품질 저하가 드러날 수 있다.

---

## 3. 속도 · 메모리 (이 앱의 실제 조건: CPU 추론)

- **CPU 처리량:** E2B ≈ 5-10 tok/s, E4B ≈ 2-5 tok/s (하드웨어 의존, 대략치). 여러 벤치에서 **E2B가 E4B보다 44~82% 빠름** — 병목은 메모리 대역폭이라 파라미터가 작을수록 유리.
- **TTFT(첫 토큰 지연):** 이 저장소의 레이턴시 최적화 방향(고정 system 프롬프트 KV 재사용 등)과 결이 같다. E2B는 가중치가 작아 콜드 로딩·프리필도 빨라 **체감 반응성**이 확연히 개선된다.
- **메모리:** E4B(5.4GB) → E2B(Q4 ~2.5-3GB)로 교체 시 **상주 약 2.5-3GB 절감**. 앞서 넣은 저메모리 모드(유휴 언로드·mlock off)와 결합하면 저사양에서 여유가 크게 생긴다.

---

## 4. 이 앱 기능별 적합성

| 기능 | E2B로 낮췄을 때 영향 |
|---|---|
| 일반 채팅 / 짧은 요약 | 대체로 무난. 속도 이득이 커서 체감 향상 가능 |
| 긴 문서·회의록 심층 요약 | 품질 저하 위험(추론 격차 ~9%p). 핵심 누락·뭉개짐 가능성 |
| 한국어 뉘앙스 | 일반적으로 파라미터 큰 쪽이 비영어에서 덜 무너짐 → **E4B가 안전**(한국어 직접 벤치는 공개 데이터 없음, 추정) |
| 이미지/스캔 PDF OCR(통합 비전) | 멀티모달 격차 큼(+8.4%p). **E2B는 OCR 정확도 저하 뚜렷할 수 있음** |
| RAG(파일 검색 답변) | 검색 자체는 임베딩(BGE-M3) 담당이라 영향 적음. 답변 종합 품질만 소폭 하락 |

> 참고: 통합 멀티모달 모드에서는 텍스트·이미지를 같은 모델이 처리하므로, E2B로 낮추면 **채팅뿐 아니라 OCR 품질도 함께 내려간다**. 이미지 문서 비중이 높다면 이 점이 결정적이다.

---

## 5. 권장안

**A. 저사양(16GB급) · 속도 우선 → E2B 전환**
- 상주 ~2.5-3GB 절감 + CPU 응답 ~2배. 저메모리 모드와 함께 쓰면 유휴 footprint가 1-2GB대로 내려감.
- 대가: 심층 요약·한국어 뉘앙스·OCR에서 품질 하락 감수.

**B. 품질 우선 · RAM 여유 → E4B 유지**
- 지금 구성 그대로. 저메모리 모드(유휴 언로드)로 유휴 시 메모리만 회수.

**C. 절충 — 권장 실험 순서**
1. 설정에서 textModel(및 통합 모드면 visionModel)을 **E2B Q4_K_M**으로 교체
2. 실제 문서로 요약·OCR·한국어 채팅을 며칠 사용해 체감 품질 확인
3. 요약 누락/OCR 오류가 거슬리면 E4B로 복귀 — 앱은 경로 교체만으로 전환되므로 리스크 낮음

> 팁: 텍스트 채팅은 E2B로도 충분한 경우가 많고, 품질이 정말 중요한 건 보통 **이미지 OCR과 장문 요약**이다. 이미지 문서를 거의 안 다룬다면 E2B 전환의 부담이 크게 준다.

---

## 6. 전환 방법 (실무)

1. E2B GGUF 확보: `unsloth/gemma-4-E2B-it-GGUF` 등에서 `Q4_K_M` 파일을 `.Models`에 배치
2. 통합 비전을 쓸 경우 E2B용 **mmproj**도 함께 확보(E4B용과 별개)
3. 설정 → AI 캐싱에서 textModel(및 visionModel)을 E2B로 지정
4. 저메모리 모드 토글과 병행하면 효과 극대화

---

## 부록 · 데이터 신뢰도 메모

- 벤치마크 수치는 모델카드/집계 사이트 기반이며 출처 간 편차가 있어 **상대 격차 중심**으로 해석했다.
- 한국어 전용 E2B vs E4B 직접 비교 벤치는 공개 데이터에서 확인되지 않아, 다국어 일반 경향(대형이 비영어에서 유리)으로 추정 표기했다.
- CPU tok/s는 하드웨어(코어·메모리 대역폭)에 크게 좌우되는 대략치다.
