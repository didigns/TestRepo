{
  "name": "aisummary-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "AISummary Tauri desktop shell (loads the local FastAPI UI)",
  "scripts": {
    "tauri": "tauri",
    "dev": "node dev.mjs",
    "dev:app-only": "tauri dev",
    "dev:backend": "python -m aisummary.api",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
